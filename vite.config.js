// vite.config.js
import { defineConfig } from 'vite'

export default defineConfig({
  // You can add config options here if needed
  // For example, to change the dev server port:
  // server: { port: 3000 }
})
